// ── Constants & Configuration ───────────────────────────────────────────────

export const DEFAULT_DPI = 300;
export const MM_PER_INCH = 25.4;

export const RENDERING = {
  STAMP_MIN_PX: 3,
  DITHER_THRESHOLD_DEFAULT: 128,
  PASSES_MAX: 3,
  GHOST_CAPACITY: 8,
};

export const PHYSICS = {
  BENDING_AMPLITUDE: 0.12,
  ROW_SHIFT_SCALE: 4,
};
